# ============================================================
# Editable RK Evaluation Profiles
# ------------------------------------------------------------
# This file is sourced by _UNG_DUNG/engine/rk_evaluation/evaluation.R.
# Override only the fields you want to customize. Missing fields fall back
# to built-in defaults.
# ============================================================

EVALUATION_PROFILES <- list(
  # Example override:
  # pH_H2O = list(
  #   rmse_thresholds = list(excellent = 0.20, good = 0.30, acceptable = 0.50),
  #   valid_range = list(min = 3.0, max = 10.0),
  #   soft_warning_range = list(min = 3.5, max = 9.5)
  # )
)
